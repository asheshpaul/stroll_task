import 'package:flutter/material.dart';

class ActionFooterSection extends StatelessWidget {
  const ActionFooterSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
